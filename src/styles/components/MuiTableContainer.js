import palette from '../palette';

export default {
  styleOverrides: {
    root: {
      border: '1px solid ' + palette.secondary.main,
      borderBottom: 'none',
      background: palette.primary.light,
    },
  },
};
